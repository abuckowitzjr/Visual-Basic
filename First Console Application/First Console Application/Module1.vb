Module Module1
    Dim strInput As String
    Dim intPoints As Integer
    Sub Main()
        Console.WriteLine("This is a message")
        intPoints = Integer.Parse(Console.ReadLine())
        Console.WriteLine(intPoints.ToString)
        Console.Read()
    End Sub

End Module
